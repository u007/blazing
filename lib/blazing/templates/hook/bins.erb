if [ -f "Gemfile" ] ; then
  echo "------> [blazing] deleting bins"
  bundle config --delete bin
  rm -f bin/*
  # echo "------> [blazing] regenerate bins"
  # bundle exec rake rails:update:bin
  # echo "------> [blazing] bins done!"
fi
